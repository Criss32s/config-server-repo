feign:
  hystrix:
    enabled: true

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMiliseconds: 1000
    
client:
  uri:
    coordenadas: /api/v1/bancos/estado/{state}
    estado: /api/v1/bancos/estado/{state}
    zipcode: /api/v1/bancos/zipcode/{postalcode}

controller:
  uri: /api/v1/sucursales/retrieve

message:
    badRequest: Missing or invalid Parameters
    badChannelRequest: The request operation is not configured to access this resource